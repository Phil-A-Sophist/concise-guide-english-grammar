# Claude Code OS

## Session Start

Personality and project context live in Supabase. The boot sequence loads them automatically:
1. `.supabase_fetch.py` — pulls code/, .claude/, and data/ from Supabase to disk
2. `kickstart` — loads memory (state, map, personality, log) from Supabase into context
3. After kickstart, proceed directly to assisting — do not scan local files or Supabase on startup.

## Claude Desktop (Chat)

Call `grammar_kickstart()` via MCP to load memory. If unavailable, use `default_kickstart()`.
All reads and writes go through the Supabase MCP. Drop incoming files at `data/memory/downloads/`.

## Claude Desktop (CoWork)

Call `grammar_kickstart()` via MCP to load memory. If unavailable, use `default_kickstart()`.
Read/write local files directly. `data/` is Supabase AND local — write to both.
Drop incoming files locally at `./_drop/`. See `memory-guide.md` for memory editing patterns.

> Do not edit this file without explicit user instruction.

## File Architecture

```
concise-guide-english-grammar/
├── CLAUDE.md              <- This file (permanent local — lean OS only)
├── .supabase_fetch.py     <- Permanent local — first boot, pulls everything else
├── .gitignore
├── .claude/               <- Pulled from Supabase on boot (settings, commands, hooks) — gitignored
│   ├── settings.json      <- Permissions only (no hooks — hooks are in global settings)
│   └── settings.local.json  <- Machine-specific, never Supabase, gitignored
├── code/                  <- Pulled from Supabase on boot (meta-agent) — gitignored
├── data/                  <- Supabase primary AND local + git-tracked (memory backup)
│   ├── memory/            <- state.md, project-map.md, knowledge-base, logs
│   ├── personality/       <- personality.md, project-personality.md
│   ├── living/            <- chapter-improvement-process, project-context, etc.
│   └── static/            <- archived snapshots, frozen docs
└── pretext/               <- PreTeXt XML source (single source of truth for book content)
```

## New Machine Setup

Clone repo -> set SUPABASE_URL and SUPABASE_KEY env vars -> open Claude Code -> done.

## Self-Management

- **This file:** Under 60 lines. No project content, behavior, or permissions here.
- **personality.md / project-personality.md:** Live in Supabase at data/personality/.
- **settings.json:** Permissions only. Hooks are in global ~/.claude/settings.json.

## Self-Review (every 20 tasks)

1. CLAUDE.md — anything here that belongs elsewhere? Move it.
2. project-personality.md (in Supabase) — stale or wrong? Fix it.
3. settings.json — permissions accurate? settings.local.json — machine-specific only?
4. Report changes.

## Git

**This is a live student site.** Every push to GitHub updates the public textbook immediately.
- data/ IS git-tracked (memory backup). Commit data/ changes with each task.
- code/ and .claude/ are gitignored (pulled from Supabase).
- Git pushes happen automatically via consolidate every 20 tasks.
- For chapter work: always commit chapter-specific files only. Never `git add -A`.
