{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo === MEMORY SYSTEM ACTIVE === && echo Spawn memory consolidation subagent NOW. Execute .memory/instructions.md && echo === Current State === && if exist .memory\\state.md (type .memory\\state.md) else (echo No state.md found — cold start needed)"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"if (Test-Path .git) { $status = git status --porcelain; if ($status) { echo '[GIT] Changes detected — will commit at task end.' } }\"",
            "timeout": 10
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(pretext:*)",
      "Bash(pandoc:*)",
      "Bash(findstr:*)"
    ]
  }
}
